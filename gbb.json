{
    "version": "0.6.0",
    "tool": "go install",
    "importpath": "github.com/voidint/star/build",
    "variables": [
        {
            "variable": "Build",
            "value": "{{.Date}}"
        },
        {
            "variable": "GitCommitHash",
            "value": "{{.GitCommit}}"
        }
    ]
}